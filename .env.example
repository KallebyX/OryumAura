# Oryum Aura - Environment Variables (Example)
# Copy this file to .env and update with your values

# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DB_PATH=./database.db

# JWT Configuration (OBRIGATÓRIO - Mínimo 32 caracteres)
# ATENÇÃO: O sistema NÃO iniciará sem um JWT_SECRET válido (segurança crítica)
# Gere um secret seguro com: openssl rand -base64 32
# Exemplo: JWT_SECRET=$(openssl rand -base64 32)
JWT_SECRET=CHANGE-THIS-GENERATE-WITH-OPENSSL-RAND-BASE64-32-MINIMUM-32-CHARS
JWT_EXPIRATION=8h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log
